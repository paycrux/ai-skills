# {작업 제목} - 발견사항

## 코드베이스 탐색

### 직접 관련 (키워드 매칭)

- `{파일 경로}` - {현재 동작 설명}

### 영향 범위 (import/구조 추적)

- `{파일 경로}:{라인}` - {관련 이유}

### 관련 없음 확인

- `{파일 경로}` - {검색에 걸렸지만 수정 불필요한 이유}

<!-- 아래 섹션은 프론트엔드 작업일 때만 포함 -->

## 기존 UI 컴포넌트 조사

### 재사용 가능 컴포넌트

- `{파일 경로}` - {컴포넌트명}, {역할}, {현재 props 요약}

### 프로젝트 UI 컨벤션

- 상태관리: {useState / zustand / redux / react-query 등}
- 스타일링: {CSS Modules / styled-components / Tailwind 등}
- 디자인 토큰: {경로 또는 "미사용"}
- 라우팅: {react-router / Next.js pages 등}

### 라이브러리 & 기존 구현 패턴

> 이번 작업에서 필요한 기능(form, table, modal 등)이 기존 코드베이스에서 어떤 라이브러리로, 어떤 패턴으로 구현되어 있는지 조사한다. 새로 구현할 때 이 패턴을 따를 것.

| 기능 영역           | 사용 라이브러리       | 기존 구현 예시 | 패턴 요약                             |
| ------------------- | --------------------- | -------------- | ------------------------------------- |
| {예: form}          | {예: react-hook-form} | `{파일 경로}`  | {validation 방식, submit 흐름 등}     |
| {예: data fetching} | {예: tanstack-query}  | `{파일 경로}`  | {query key 컨벤션, error handling 등} |

## 기술 결정

| 결정 | 대안 | 선택 이유 |
| ---- | ---- | --------- |
