{
  "name": "ai-skills-browse",
  "version": "1.0.0",
  "description": "Headless browser for QA testing. Based on gstack browse (MIT).",
  "type": "module",
  "scripts": {
    "build": "bun build --compile src/cli.ts --outfile dist/browse && chmod +x dist/browse",
    "dev": "bun run src/cli.ts"
  },
  "dependencies": {
    "diff": "^7.0.0",
    "playwright": "^1.58.2"
  }
}
