---
name: {skill-name}
description: "{what it does}. {when to trigger}."
argument-hint: "{expected-input}"
---

# /{skill-name} — {One-line Purpose}

{Brief overview in 1-2 sentences.}

## Argument Parsing

- `/{skill-name}` — {default behavior}
- `/{skill-name} <arg>` — {with argument behavior}

## Execution

### Step 1: {Action Name}

{What Claude does in this step.}

### Step 2: {Action Name}

{What Claude does in this step.}

## Output Format

Use the template at `${CLAUDE_SKILL_DIR}/templates/{output-template}.md`.
Read the template file and fill in all `{placeholder}` values.

## Rules

- {Hard constraint 1}
- {Hard constraint 2}
